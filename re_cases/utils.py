import random
# 随机生成汉字
def get_hans(num):
    str=""
    # for i in range(num):
    #     head = random.randint(0xb0, 0xf7)
    #     body = random.randint(0xa1, 0xfe)
    #     val = f'{head:x}{body:x}'
    #     str = str+bytes.fromhex(val).decode('gb2312')
    return "str"